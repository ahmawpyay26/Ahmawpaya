# ============================================
# Environment Variables for Production
# အမောပြေ (Ah-Maw-Pyay) Water Delivery App
# ============================================
# Copy this file to .env in the project root:
#   cp docker/env.template .env
# Then fill in your actual values.
# ============================================

# --- Database Configuration ---
DB_ROOT_PASSWORD=StrongRootPassword123!
DB_NAME=amaw_pyay
DB_USER=amaw_user
DB_PASSWORD=AmawPyay2024!

# --- Application ---
APP_PORT=3000
JWT_SECRET=generate-a-random-64-char-string-here

# --- OAuth (Manus) ---
# These are only needed if using Manus OAuth for admin login
VITE_APP_ID=
OAUTH_SERVER_URL=
VITE_OAUTH_PORTAL_URL=
OWNER_OPEN_ID=
OWNER_NAME=

# --- Manus Forge API (Optional) ---
# Only needed if using Manus built-in services (LLM, notifications, etc.)
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
